<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AAirbnb </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Reset default browser margins and paddings for html and body */
        html,
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            /* Ensure html and body take full height */
        }

        body {
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            /* Ensure body takes full viewport height */
        }
    </style>
</head>

<body class="antialiased <%= isLoggedIn ? 'bg-white' : 'bg-gray-900' %>">
    <nav class="flex flex-col sm:flex-row sm:justify-between items-center w-full p-4 bg-blue-700 text-white shadow-lg">
        <div class="flex items-center mb-4 sm:mb-0">
            <% if(isLoggedIn && user && user.username){ %>
                <div class="w-10 h-10 bg-blue-500 rounded-full flex items-center justify-center mr-4 text-xl font-bold uppercase ring-2 ring-white hover:bg-blue-400 transition-colors duration-200 cursor-pointer"
                    title="<%= user.username %>">
                    <%= user.username.charAt(0) %>
                </div>
            <% } %>
            <a href="/" class="text-3xl font-bold hover:text-gray-300 transition-colors duration-200">Airbnb</a>
        </div>

        <div class="flex flex-wrap justify-center sm:justify-end items-center space-x-4 sm:space-x-6">
            <a href="/home"
                class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                <i class="fas fa-home mr-2"></i>Home
            </a>

            <% if(isLoggedIn){ %>
                <% if(user && user.usertype==='guest'){%>

                    <a href="/favourite"
                        class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                        <i class="fas fa-heart mr-2"></i>Favourite
                    </a>
                    <a href="/booking"
                        class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                        <i class="fas fa-book-open mr-2"></i>Bookings
                    </a>

                <% } else{%>
                    <a href="/home/add-home"
                        class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                        <i class="fas fa-plus-square mr-2"></i>Add Home
                    </a>
                    <a href="/host-home-list"
                        class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                        <i class="fas fa-hotel mr-2"></i>Host-Home
                    </a>

                <%}%>
                <a href="/explore"
                    class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                    <i class="fas fa-compass mr-2"></i>Explore
                </a>
                <a href="/about"
                    class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                    <i class="fas fa-info-circle mr-2"></i>About
                </a>
            <% } %>

            <% if(isLoggedIn){%>
                <a href="/logout"
                    class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                    <i class="fas fa-sign-out-alt mr-2"></i>Logout
                </a>
                <a href="/profile"
                    class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                    <i class="fas fa-sign-in-alt mr-2"></i>Profile
                </a>
            <% }else{%>
                <a href="/login"
                    class="text-lg font-medium hover:text-gray-400 transition-colors duration-200 py-1 px-2 rounded-md">
                    <i class="fas fa-sign-in-alt mr-2"></i>LogIn
                </a>
            <% }%>
            
        </div>
    </nav>
</body>

</html>